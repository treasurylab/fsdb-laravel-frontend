import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fourthsignal.db',
  appName: 'FsDataBridge',
  webDir: 'dist/fs-data-bridge',
  bundledWebRuntime: false
};

export default config;
