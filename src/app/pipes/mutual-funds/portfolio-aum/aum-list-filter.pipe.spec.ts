import { AumListFilterPipe } from './aum-list-filter.pipe';

describe('AumListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AumListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
