import { LogInfoFilterPipe } from './log-info-filter.pipe';

describe('LogInfoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LogInfoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
