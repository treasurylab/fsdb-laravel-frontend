import { MappedClassIdFilterPipe } from './mapped-class-id-filter.pipe';

describe('MappedClassIdFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MappedClassIdFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
