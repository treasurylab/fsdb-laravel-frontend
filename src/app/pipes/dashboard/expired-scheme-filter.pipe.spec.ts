import { ExpiredSchemeFilterPipe } from './expired-scheme-filter.pipe';

describe('ExpiredSchemeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpiredSchemeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
